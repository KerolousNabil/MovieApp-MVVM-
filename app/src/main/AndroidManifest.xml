<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.moviesland">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MoviesLand"
        tools:targetApi="31">
        <activity
            android:name=".Ui.movie_data.MovieDetailsActivity"
            android:exported="false" />
        <activity
            android:name=".Ui.image_result_zoom.ImageViewerActivity"
            android:exported="false" />
        <activity
            android:name=".Ui.person_data.PersonDetailsActivity"
            android:exported="false" />
        <activity
            android:name=".Ui.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>